import React from 'react';
import { Typography } from 'antd';

const { Title, Text } = Typography;

const CounterPartySetup: React.FC = () => {
  return (
    <div style={{ padding: 16 }}>
      <Title level={4}>Counter Party Setup</Title>
      <Text type="secondary">Counter Party Setup page - Under construction</Text>
    </div>
  );
};

export default CounterPartySetup;
