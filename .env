# 开发模式（连真实后端联调）。
# 启用此模式：`npm run dev`（默认加载本文件）
#
# 后端需部署在 http://127.0.0.1:8080/settocbcBoServer
# 登录后依赖 GetSessionKeyServlet 取 sessionKey/version（见 01_SessionAuth.md）。

# 真实请求 URL（默认即此值，列出便于改环境）
VITE_PULL_URL=/settocbcBoServer/BOAppPullServlet
VITE_LOGIN_URL=/settocbcBoServer/BOAppLoginServlet
VITE_GET_SESSION_KEY_URL=/settocbcBoServer/GetSessionKeyServlet

# mock 关闭
VITE_USE_MOCK=false
